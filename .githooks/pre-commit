#!/bin/sh
set -e
cargo fmt --all -- --check 2>&1 | tail -3
echo "--- cargo clippy ---"
cargo clippy --workspace --all-targets -- -D warnings -A clippy::collapsible_if -A clippy::collapsible_match 2>&1 | tail -5
